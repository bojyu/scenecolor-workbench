# SceneColor 生成队列、落盘与预览优化方案

日期：2026-07-30

## 1. 目标

本次优化解决四类直接影响生产使用的问题：

1. 点击“提交生成”后，工作台仍可继续选择场景、素材和参数，并把下一批任务追加到后台队列。
2. 每次生成和重做都有不可变版本；失败的重做不能覆盖或隐藏此前已成功的图片。
3. 服务端确认图片完整落盘后，前端才把该版本标记为成功；刷新页面后可从项目目录恢复历史。
4. 人工选择和结果浏览只加载服务端缩略图，下载、核验和后续处理继续使用原图。

## 2. 原问题与根因

### 2.1 提交逻辑是页面级线性锁

旧流程用一个全局“生成中”状态锁住提交与部分编辑控件。一批任务未全部结束前，用户无法自然追加下一批；重试和重做还走独立逻辑，状态容易相互覆盖。

### 2.2 结果文件会发生路径碰撞

旧文件名只包含产品组/颜色和场景名。同一组内的不同产品参考图会写到相同输出路径；重做版本又可能从 `v1` 重新计算，导致已有文件被覆盖。历史记录因此可能指向缺失文件或另一张图片。

### 2.3 浏览器状态承担了不该承担的持久化

完整 Base64 图片进入 React/localStorage 后，页面内存、序列化和恢复都很重。刷新后 Base64 被清理时，预览又会消失。客户端请求 ID 还可能被误当成后端核验记录 ID。

### 2.4 扫描和预览读取原图

扫描历史时同步读取并哈希全部 4K 输出，人工选择时也可能加载大图。图片多时，工作台需要等待大量磁盘 I/O 和解码，造成“卡住”“图片少了”的感知。

## 3. 优化后的数据与执行模型

### 3.1 可追加后台队列

- 点击提交只负责创建一批任务快照并入队，不等待整批结束。
- 快照固定记录场景、主参考图、辅助参考图、提示词、模型、比例和分辨率。
- 固定数量的后台 Worker 从 FIFO 队列取任务；用户可继续调整选择并追加新批次。
- 同一个“场景 + 产品”已有活动任务时拒绝重复入队；输入变化后可形成新版本。
- 停止只取消活动/排队请求，不删除已经成功的版本。

### 3.2 不可变尝试与版本

- 每次生成、重试和重做都创建独立 attempt。
- attempt 有客户端 `requestId`；只有服务端成功写入项目记录后才有可核验的 `attemptId`，二者不能互相冒充。
- 重做先保留旧成功图；新版本成功后切换，新版本失败则继续展示旧成功图并显示失败原因。
- 界面可切换已成功的历史版本；下载和核验使用当前人工选择的版本。

### 3.3 原子落盘

- 输出文件名包含产品组、具体产品素材名、场景名和单调版本号。
- 路径过长时使用稳定哈希压缩文件名，规避 Windows 路径问题。
- 生成结果先转换成真实 PNG 并完整写入临时文件，再以不可覆盖方式原子发布。
- 同一输出族串行分配版本，任何并发请求都不能覆盖既有文件。
- API 只在落盘完成后返回成功、实际路径、实际版本、尺寸和服务端 attempt ID。

### 3.4 项目目录是持久化真相

- 每次成功结果在 `<项目>/.scenecolor/generation-attempts/` 保存不可变记录。
- 记录包含生成开始时实际送给模型的输入 Hash、输出 Hash、模型、比例、分辨率和版本。
- 全局索引只是可重建缓存；核验可直接用项目本地记录恢复，不能因应用启动目录变化而失效。
- 页面刷新先快速读取记录元数据并显示工作台，再在后台用低并发做输出完整性审计。
- 审计发现文件缺失或 Hash 不一致时，立即把对应版本标记为无效，禁止核验或误当成功图。

### 3.5 缩略图与原图分流

- 场景列表、素材列表、人工对比弹窗、结果卡片统一使用按宽度生成的 JPEG 缩略图。
- 缩略图接口提供 ETag、浏览器缓存、服务端 LRU 和同请求合并。
- React 状态和 localStorage 不保存 4K Base64。
- 下载、核验和细节重绘始终引用不可变原始 PNG 路径，不使用缩略图。
- 缩略图请求失败时显示明确的重试入口，不让图片区域静默消失。

### 3.6 生成请求体预算

- 单张参考图先按尺寸和编码压缩。
- 场景图、主参考图和辅助参考图还要共同满足整包字节预算，避免四张图片分别合格但 Base64 JSON 总体触发 413。
- 原始 Buffer 继续用于几何分析、缓存键和生成时输入 Hash；压缩副本只用于网络传输。

## 4. 状态约束

1. `success => savedPath 存在且服务端已完成原子发布`。
2. 同一输出族的成功版本号只能递增，已有文件永不覆盖。
3. 重做失败不能删除、替换或隐藏最后一次成功结果。
4. 项目切换会取消旧项目活动任务；同项目重新扫描不能覆盖活动任务的 CAS 状态。
5. 磁盘完整性结果优先于 localStorage 缓存。
6. 没有真实服务端 `attemptId` 的版本不能进入自动核验。
7. 工作台渲染只消费缩略图；原图只在下载、生成、核验和后处理时读取。

## 5. 验收场景

- 一批正在生成时，修改选择并成功追加第二批；两批结果均完整落盘。
- 同一组合连续重做三次得到单调版本；中间一次失败时前一成功版本仍可预览和下载。
- 生成中重新扫描同一项目，活动任务仍能正常回写 UI。
- 刷新页面后恢复所有成功版本；排队/运行中的旧请求显示为已停止，不伪装成完成。
- 手动删除或篡改一个输出文件后刷新，历史版本被标记为缺失/不一致，且不能发送核验。
- 关闭或损坏全局 attempt 索引后，仍可从项目本地记录执行核验并重建索引。
- 使用四张高熵大图生成时，请求体保持在整包预算内。
- 人工素材选择和结果卡片的图片 URL 均为缩略图接口；下载得到原始 PNG。

## 6. 后续可选优化

- 任务监控区增加真正的“批次视图”，按提交时间展开每批任务，而不仅按场景/产品组合聚合。
- 为不同模型维护独立 Worker 池，让运行中切换模型和 API Key 后的新批次立即执行。
- 将完整性审计改为基于文件大小、修改时间的快速短路，仅在元数据变化时重新计算 Hash。
- 增加项目级队列事件日志，便于定位外部进程改图、磁盘空间不足和进程异常退出。
